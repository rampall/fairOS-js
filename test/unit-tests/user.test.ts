import "jest";
import { FairOS } from "../../src";

describe("User", () => {
  jest.setTimeout(30000);

  beforeAll(() => {});

  it("", async () => {
    const fairOS = new FairOS({ baseUrl: "http://127.0.0.1" });
  });
});
